import EventLog from "./EventLog";

export default function ActivityLogsSection() {
  return <EventLog title="Activity Logs" description="Chronological record of everything happening in Terra Explorer." showStats={false} />;
}
