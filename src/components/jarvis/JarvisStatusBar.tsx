import { Activity, Cpu, MousePointer2, ShieldCheck } from "lucide-react";
export default function JarvisStatusBar({ quality, modelReady, online }: { quality: string; modelReady: boolean; online: boolean }) { return <div className="flex flex-wrap items-center gap-x-5 gap-y-2 border-t border-border/40 bg-background/70 px-4 py-2 text-[10px] font-medium uppercase tracking-wider text-muted-foreground backdrop-blur-xl"><span className="flex items-center gap-1.5"><Activity className="h-3 w-3 text-primary" />Telemetry nominal</span><span className="flex items-center gap-1.5"><Cpu className="h-3 w-3 text-primary" />Quality {quality}</span><span className="flex items-center gap-1.5"><MousePointer2 className="h-3 w-3 text-primary" />Orbit controls active</span><span className="flex items-center gap-1.5"><ShieldCheck className="h-3 w-3 text-primary" />{online ? "Cloud sync ready" : "Local workspace"}</span><span className="ml-auto">{modelReady ? "Reconstruction loaded" : "Blueprint fallback"}</span></div>; }
